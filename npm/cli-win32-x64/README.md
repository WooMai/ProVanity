# @provanity/cli-win32-x64

This is an optional platform package for `provanity`.

It contains the Windows x64 `provanity.exe` binary and is installed automatically by the main `provanity` package when the current platform matches.

Install `provanity` instead:

```sh
npm install -g provanity
```
