# @provanity/cli-linux-x64

This is an optional platform package for `provanity`.

It contains the Linux x64 `provanity` binary and is installed automatically by the main `provanity` package when the current platform matches.

Install `provanity` instead:

```sh
npm install -g provanity
```
